---
hadoop_masters:
  - localhost
hadoop_slaves:
  - localhost
accumulo_example_config: 512MB/native-standalone
accumulo_masters:
  - %{ipaddress_eth1}
accumulo_slaves:
  - %{ipaddress_eth1}
zookeeper_nodes:
  1: %{ipaddress_eth1}:2181
namenode_ipaddress: %{ipaddress_eth1}
elasticsearch_publishaddress: %{ipaddress_eth1}
