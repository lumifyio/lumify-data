[
  {rabbit, [
    <%- if @rabbitmq_nodes != nil && @rabbitmq_nodes.count > 1 %>
    {cluster_nodes, {[<%= @rabbitmq_nodes.collect {|hostname| "'rabbit@#{hostname}'"}.join(', ') %>], disc}}
    <%- end %>
  ]}
].
