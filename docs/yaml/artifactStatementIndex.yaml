---
table:
    label: ArtifactStatementIndex
    description: Index of Statements by Artifact

rowKey:
    artifact row id:    sha256 - Hash of the Artifact being indexed

columnFamilies:
    statement row id:
        statement table column family name: string - The row id and column qualifier reference column family containing statement

connections:
    Statement:  many -> many
    Artifact:   many -> none
...