---
table:
    label: ArtifactPublicationDateIndex
    description: Search Artifacts by date range

rowKey:
    timestamp:  long - Unix milliseconds timestamp

columnFamilies:
    ArtifactId:
        artifact row id (sha256 hash):

connections:
    Artifact: one -> one
...