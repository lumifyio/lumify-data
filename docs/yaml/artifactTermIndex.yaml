---
table:
    label: ArtifactTermIndex
    description:

rowKey:
    artifact row id:    sha256 - Hash of Artifact being indexed

columnFamilies:
    term row id:
        Term table column family name:  string - The row id and column qualifier reference the column family containing the mention

connections:
    Artifact:   many -> one
    Term:       many -> many
...