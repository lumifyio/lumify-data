---
table:
    label: atc_ArtifactObjectIndex
    description:

rowKey:
    artifact row id:    sha256 - Hash of Artifact being indexed

columnFamilies:
    object row id:
        Object table column family name:  string - The row id and column qualifier reference the column family containing the mention

connections:
    Artifact:   many -> one
    Object:       many -> many
...
