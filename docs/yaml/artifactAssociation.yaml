---
table:
    label: ArtifactAssociation
    description: Asserted relationships between artifacts

rowKey:
    Subject Artifact row id:    sha256 - The hash of the subject artifact

columnFamilies:
    predicateKey (relationship between artifacts):
        Object Artifact row id (sha256 hash):   json - Metadata about the relationship (date, author, security marking)

connections:
...