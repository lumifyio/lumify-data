---
table:
    label: atc_VideoFrame
    description:

rowKey:
    artifactRowKey:     sha256 - Row key of the Artifact that generated the VideoFrame
    frameStartTime:     leftPad16 - The time in the video where the VideoFrame occurs, left padded with zeros to 16 digits

columnFamilies:
    metadata:
        hdfs_path:  string - HDFS URI of the VideoFrame data
        text:       string - Text extracted from the VideoFrame
        start_time: string - The start time of the VideoFrame
        end_time:   string - The end time of the VideoFrame

connections:
    Artifact:   many -> one
...