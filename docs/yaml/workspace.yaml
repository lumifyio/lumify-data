---
table:
    label: atc:Workspace
    description: Persists user workspaces

rowKey (separated by \x1F):
    userId:         string - The ID of the user the Workspace belongs to
    workspaceId:    long - The ID of the Workspace

columnFamilies:
    content:
        data:       json - The data held in the Workspace (comprised of nodes)
    metadata:
        title:      string - The title of the Workspace

connections:
...