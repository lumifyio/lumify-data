---
table:
    label: Object
    description:

rowKey (separated by \x1F):
    sign:           string - The name of the Object
    modelKey:       string - The model used to generate the Object
    conceptLabel:   string - The concept associated with the Object (e.g. person)

columnFamilies:
    ObjectStatement:
        Statement row id:   string - Term being indexed

connections:
    Statement:  many -> many
...