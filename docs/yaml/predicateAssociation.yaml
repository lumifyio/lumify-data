---
table:
    label: PredicateAssociation
    description: Predicates that relate two Predicates

rowKey:
    subject predicate key:  string - The row key of the subject Predicate

columnFamilies:
    relationship predicate id:
        object predicate id:    json - Metadata about Predicate relationship (date, author, security marking)
    Dynamic_Metadata:

connections:

...